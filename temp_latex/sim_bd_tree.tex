\chapter{Simulation of birth-death tree and TDA detection}

There are two directions under this project. Since they both rely on the simulation of a stochastic birth-death tree, they are grouped here together.

The pseudo-code for the simulation of the birth-death tree is shown below:
\section{Simulation of the birth-death tree}
\begin{algorithm}[H]
\caption{Birth--Death--Hybridization (BDH) simulation}
\begin{algorithmic}[1]
\State Initialize $G$ with root and $1$--$2$ starting leaves; $t \gets 0$
\While{$|\mathcal{L}| > 0$ and $|\mathcal{L}| < N_{\text{stop}}$}
    \State $n \gets |\mathcal{L}|$
    \State $r_{\text{spec}} \gets n\lambda$,\quad $r_{\text{ext}} \gets n\mu$,\quad $r_{\text{hyb}} \gets \binom{n}{2}\nu$
    \State $r_{\text{tot}} \gets r_{\text{spec}} + r_{\text{ext}} + r_{\text{hyb}}$
    \State draw $\Delta t \sim \text{Exp}(r_{\text{tot}})$; \quad $t \gets t + \Delta t$
    \If{$t \geq \text{age}$}
        \State $t \gets \text{age}$; \textbf{break}
    \EndIf
    \State sample $u \in \mathcal{L}$ uniformly; draw $\rho \sim \text{Unif}(0,1)$
    \If{$\rho < r_{\text{spec}} / r_{\text{tot}}$}
        \State \textsc{Speciate}$(u)$ \Comment{$u \to$ two new leaves}
    \ElsIf{$\rho < (r_{\text{spec}} + r_{\text{ext}}) / r_{\text{tot}}$}
        \State \textsc{Extinguish}$(u)$ \Comment{mark $u$ extinct, remove from $\mathcal{L}$}
    \Else
        \State sample $v \in \mathcal{L} \setminus \{u\}$; draw inheritance $\iota \sim \pi_{\text{inher}}$
        \State draw event type $\in \{\text{generating}, \text{degenerative}, \text{neutral}\}$ w.p. $\propto$ \textsc{hybprops}
        \State \textsc{Hybridize}$(u, v, \iota, \text{type})$ \Comment{merge lineages, add reticulation edge(s)}
    \EndIf
\EndWhile
\State seal all pendant edges to current leaves
\State \Return $G$
\end{algorithmic}
\end{algorithm}

\section{Detection of reticulation edge in the simulated trees}

Once the tree is produced, it needs to be populated to accurately capture its topology. 

\section{Detection of reticulation edges in the union of gene trees}
The previous section produces a network with reticulate edges present. In this section, we produce a set of gene trees based on the reticulated network and attempt to detect the reticulate edges in the union of the gene trees. 

Different from the previous project, the gene trees do not need to be populated because there are only edges of weight 1.

To illustrate the full pipeline, we use a small toy network throughout this section. The reticulation node $H$ has in-degree 2, receiving an edge from both $A_2$ and $B$ --- both edges are present simultaneously in the network as simulated, before any filtering or gene-tree resolution takes place:

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    leaf/.style={circle,draw,fill=gray!30,minimum size=6mm,inner sep=0pt},
    internal/.style={circle,draw,fill=white,minimum size=6mm,inner sep=0pt}
]
\node[internal] (R)  at (0,0)      {$R$};
\node[internal] (X)  at (-1.2,-1.2){$X$};
\node[internal] (B)  at (1.2,-1.2) {$B$};
\node[leaf]     (A1) at (-2,-2.4)  {$A_1$};
\node[internal] (A2) at (-0.4,-2.4){$A_2$};
\node[internal] (H)  at (-0.4,-3.6){$H$};
\node[leaf]     (C)  at (-0.4,-4.8){$C$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (B);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (A2);
\draw[-latex] (A2) -- (H);
\draw[-latex] (B) to[bend left=20] (H);
\draw[-latex] (H) -- (C);
\end{tikzpicture}
\caption{The original simulated network: 7 nodes, one reticulation. $H$ has in-degree 2, fed by both $A_2\!\to\!H$ and $B\!\to\!H$. This is the network gene tree 1 and gene tree 2 (below) are each a single resolution of.}
\end{figure}

For each network, we decompose the network into a set of gene trees (as shown below). We first filter the network so that it no longer has hybrid nodes and extinct nodes. Then we enumerate the output to the set of gene trees, each of which takes a specific combination of all gene selections. We show two examples of the gene trees which differ because of their different choices in the event of hybridisation. During enumeration, we remove any dead ends (hanging nodes that were not labelled $"is\_leaf"$) iteratively and we remove any nodes with only one out-going nodes. 
\begin{figure}[h]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={circle,draw,fill=gray!30,minimum size=6mm,inner sep=0pt},
    internal/.style={circle,draw,fill=white,minimum size=6mm,inner sep=0pt}
]
\node[internal] (R)  at (0,0)      {$R$};
\node[internal] (X)  at (-1.2,-1.2){$X$};
\node[leaf]     (B)  at (1.2,-1.2) {$B$};
\node[leaf]     (A1) at (-2,-2.4)  {$A_1$};
\node[internal] (A2) at (-0.4,-2.4){$A_2$};
\node[internal] (H)  at (-0.4,-3.6){$H$};
\node[leaf]     (C)  at (-0.4,-4.8){$C$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (B);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (A2);
\draw[-latex] (A2) -- (H);
\draw[-latex] (H) -- (C);
\end{tikzpicture}

\small Gene tree 1: $A_2\!\to\!H$ retained --- $C$ nests deep in the $X$ clade; $A_2$ becomes internal, $B$ stays a leaf
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={circle,draw,fill=gray!30,minimum size=6mm,inner sep=0pt},
    internal/.style={circle,draw,fill=white,minimum size=6mm,inner sep=0pt}
]
\node[internal] (R)  at (0,0)      {$R$};
\node[internal] (X)  at (-1.2,-1.2){$X$};
\node[internal] (B)  at (1.2,-1.2) {$B$};
\node[leaf]     (A1) at (-2,-2.4)  {$A_1$};
\node[leaf]     (A2) at (-0.4,-2.4){$A_2$};
\node[internal] (H)  at (1.2,-2.4) {$H$};
\node[leaf]     (C)  at (1.2,-3.6) {$C$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (B);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (A2);
\draw[-latex] (B) -- (H);
\draw[-latex] (H) -- (C);
\end{tikzpicture}

\small Gene tree 2: $B\!\to\!H$ retained --- $C$ attaches at the root level, sister to the whole $X$ clade; $B$ becomes internal, $A_2$ stays a leaf
\end{minipage}
\caption{Two gene-tree resolutions of the same 7-node network, differing by which edge into the reticulate node $H$ is kept. Gray nodes are leaves, white nodes are internal.}
\end{figure}

\begin{figure}[h]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={circle,draw,fill=gray!30,minimum size=6mm,inner sep=0pt},
    internal/.style={circle,draw,fill=white,minimum size=6mm,inner sep=0pt}
]
\node[internal] (R)  at (0,0)      {$R$};
\node[internal] (X)  at (-1.2,-1.2){$X$};
\node[leaf]     (B)  at (1.2,-1.2) {$B$};
\node[leaf]     (A1) at (-2,-2.4)  {$A_1$};
\node[leaf]     (C)  at (-0.4,-2.4){$C$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (B);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (C);
\end{tikzpicture}

\small Gene tree 1 (suppressed): $A_2$ and $H$ both had exactly one outgoing edge, so they contract away in sequence --- $X\!\to\!A_2\!\to\!H\!\to\!C$ collapses to a single edge $X\!\to\!C$
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={circle,draw,fill=gray!30,minimum size=6mm,inner sep=0pt},
    internal/.style={circle,draw,fill=white,minimum size=6mm,inner sep=0pt}
]
\node[internal] (R)  at (0,0)      {$R$};
\node[internal] (X)  at (-1.2,-1.2){$X$};
\node[leaf]     (C)  at (1.2,-1.2) {$C$};
\node[leaf]     (A1) at (-2,-2.4)  {$A_1$};
\node[leaf]     (A2) at (-0.4,-2.4){$A_2$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (C);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (A2);
\end{tikzpicture}

\small Gene tree 2 (suppressed): $B$ and $H$ both had exactly one outgoing edge, so they contract away in sequence --- $R\!\to\!B\!\to\!H\!\to\!C$ collapses to a single edge $R\!\to\!C$
\end{minipage}
\caption{The two gene trees from the previous figure after iteratively removing every node with only one outgoing edge. Both $A_2/H$ (tree 1) and $B/H$ (tree 2) disappear, leaving 5-node trees where $C$ attaches one level higher than before --- as a child of $X$ in tree 1, and as a direct child of $R$ in tree 2.}
\end{figure}

After the gene trees are generated, we start labelling them using unions of leaves. 

\begin{figure}[h]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={draw,rounded corners,fill=gray!25,inner sep=2pt,font=\scriptsize},
    internal/.style={draw,rounded corners,fill=white,inner sep=2pt,font=\scriptsize}
]
\node[internal] (R)  at (0,0)       {$\{A_1,B,C\}$};
\node[internal] (X)  at (-1.2,-1.2) {$\{A_1,C\}$};
\node[leaf]     (B)  at (1.5,-1.2)  {$\{B\}$};
\node[leaf]     (A1) at (-2.3,-2.4) {$\{A_1\}$};
\node[leaf]     (C)  at (-0.1,-2.4) {$\{C\}$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (B);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (C);
\end{tikzpicture}

\small Gene tree 1
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\begin{tikzpicture}[
    leaf/.style={draw,rounded corners,fill=gray!25,inner sep=2pt,font=\scriptsize},
    internal/.style={draw,rounded corners,fill=white,inner sep=2pt,font=\scriptsize}
]
\node[internal] (R)  at (0,0)       {$\{A_1,A_2,C\}$};
\node[internal] (X)  at (-1.5,-1.2) {$\{A_1,A_2\}$};
\node[leaf]     (C)  at (1.5,-1.2)  {$\{C\}$};
\node[leaf]     (A1) at (-2.3,-2.4) {$\{A_1\}$};
\node[leaf]     (A2) at (-0.5,-2.4) {$\{A_2\}$};

\draw[-latex] (R) -- (X);
\draw[-latex] (R) -- (C);
\draw[-latex] (X) -- (A1);
\draw[-latex] (X) -- (A2);
\end{tikzpicture}

\small Gene tree 2
\end{minipage}
\caption{Both gene trees relabelled by the union of leaves under each node, in preparation for merging them into one graph.}
\end{figure}

\texttt{tree\_addition.py} merges two trees by identifying nodes with identical leaf-set labels. Here only $\{A_1\}$ and $\{C\}$ match across the two relabelled trees above --- $\{A_1,B,C\}$ and $\{A_1,A_2,C\}$ are different sets, so the two roots stay distinct, as do the two $X$ nodes and the tree-specific leaves $B$ and $A_2$. Merging on the shared leaves alone is already enough to close a cycle:

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    leaf/.style={draw,rounded corners,fill=gray!25,inner sep=2pt,font=\scriptsize},
    internal/.style={draw,rounded corners,fill=white,inner sep=2pt,font=\scriptsize},
    shared/.style={draw,rounded corners,fill=cyan!25,inner sep=2pt,font=\scriptsize}
]
\node[internal] (R1) at (-2.5,0)     {$\{A_1,B,C\}$};
\node[internal] (X1) at (-1.5,-1.3)  {$\{A_1,C\}$};
\node[leaf]     (B)  at (-4,-1.3)    {$\{B\}$};
\node[shared]   (A1) at (-0.3,-2.6)  {$\{A_1\}$};
\node[shared]   (C)  at (2.3,-2.6)   {$\{C\}$};
\node[internal] (X2) at (3.5,-1.3)   {$\{A_1,A_2\}$};
\node[internal] (R2) at (2.5,0)      {$\{A_1,A_2,C\}$};
\node[leaf]     (A2) at (5,-1.3)     {$\{A_2\}$};

\draw[-latex] (R1) -- (X1);
\draw[-latex] (R1) -- (B);
\draw[-latex] (X1) -- (A1);
\draw[-latex] (X1) -- (C);
\draw[-latex] (R2) -- (X2);
\draw[-latex] (R2) -- (C);
\draw[-latex] (X2) -- (A1);
\draw[-latex] (X2) -- (A2);
\end{tikzpicture}
\caption{Merging gene tree 1 and gene tree 2 by leaf-set identity. The shared leaves $\{A_1\}$ and $\{C\}$ (cyan) are the only merge points, and they alone are enough to close a 5-edge cycle $A_1 \!-\! X_1 \!-\! C \!-\! R_2 \!-\! X_2 \!-\! A_1$, with $R_1\!-\!B$ hanging off as a tree-like appendage. This 8-node, 8-edge graph has exactly $E-V+1=1$ independent cycle, the harmonic cycle \texttt{find\_cycles.py} is meant to detect.}
\end{figure}

